{{template "base" .}}

{{define "content"}}
<form class="row mt-5 mx-5">
  <div class="col-auto w-50">
    <label for="url" class="visually-hidden">Enter your URL:</label>
    <input type="text" class="form-control" id="url" placeholder="ex: http://www.google.com">
  </div>
  <div class="col-auto">
    <button type="submit" class="btn btn-primary mb-3">Shorten URL</button>
  </div>
</form>
{{end}}


{{define "script"}}


{{end}}